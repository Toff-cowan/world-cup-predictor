# Production (Vercel): set in Project → Settings → Environment Variables
# Must include /api at the end — your Render API URL
# VITE_API_URL=https://world-cup-predictor-api.onrender.com/api

# Local dev: use Vite proxy (leave unset) or point at local API:
# VITE_API_URL=http://localhost:5000/api
